# EMS Modbus TCP通讯调试指南（20套PCS与20套BMS）

版本：V1.1  
日期：2026-08-20  
适用工程：`D:\work\研发\ems`  
点表模板：`EMS通信点表V1.2.xlsx`

## 1. 使用范围与当前约定

本阶段先接入20套PCS和20套BMS。V1.2点表描述一套设备的数据区，EMS V3通讯配置允许每套PCS、BMS分别设置IP、TCP端口、Unit ID、超时和轮询周期，也允许PCS与BMS确实共用端点时使用`COMBINED`设备类型。

当前工程按以下方式解释现场设备：

- 通讯协议：Modbus TCP。
- 默认示例地址：`192.168.1.100:502`，现场可逐台修改。
- 读取功能码：FC03（读保持寄存器）。
- 设备清单：每行独立设置类型、IP、端口和Unit ID；未知参数的行应保持禁用。
- 当前初始配置保留Unit 1～20、PCS/BMS共用端点的兼容映射，取得现场清单后应改成真实设备行。
- EMS写控制默认禁用，不执行FC06或FC16。
- 点表中的地址按1基显示，Modbus报文PDU地址按0基发送，即“协议偏移量＝表内地址－1”。

> 现场必须确认设备是独立IP还是同一IP下多个Unit ID，以及PCS、BMS是否共用端点。不能仅凭设备编号猜测；未确认的设备先禁用，仍可保存配置。

## 2. 初始兼容设备与Unit ID对应表

| 序号 | Unit ID | PCS界面名称 | BMS界面名称 | 当前TCP端点 |
|---:|---:|---|---|---|
| 1 | 1 | PCS01 | BMS01 | 192.168.1.100:502 |
| 2 | 2 | PCS02 | BMS02 | 192.168.1.100:502 |
| 3 | 3 | PCS03 | BMS03 | 192.168.1.100:502 |
| 4 | 4 | PCS04 | BMS04 | 192.168.1.100:502 |
| 5 | 5 | PCS05 | BMS05 | 192.168.1.100:502 |
| 6 | 6 | PCS06 | BMS06 | 192.168.1.100:502 |
| 7 | 7 | PCS07 | BMS07 | 192.168.1.100:502 |
| 8 | 8 | PCS08 | BMS08 | 192.168.1.100:502 |
| 9 | 9 | PCS09 | BMS09 | 192.168.1.100:502 |
| 10 | 10 | PCS10 | BMS10 | 192.168.1.100:502 |
| 11 | 11 | PCS11 | BMS11 | 192.168.1.100:502 |
| 12 | 12 | PCS12 | BMS12 | 192.168.1.100:502 |
| 13 | 13 | PCS13 | BMS13 | 192.168.1.100:502 |
| 14 | 14 | PCS14 | BMS14 | 192.168.1.100:502 |
| 15 | 15 | PCS15 | BMS15 | 192.168.1.100:502 |
| 16 | 16 | PCS16 | BMS16 | 192.168.1.100:502 |
| 17 | 17 | PCS17 | BMS17 | 192.168.1.100:502 |
| 18 | 18 | PCS18 | BMS18 | 192.168.1.100:502 |
| 19 | 19 | PCS19 | BMS19 | 192.168.1.100:502 |
| 20 | 20 | PCS20 | BMS20 | 192.168.1.100:502 |

该表只是初始兼容值，不是现场定值。建议导出设置页JSON或打印此表，在现场逐台填写设备柜号、序列号、交换机端口和核对人。

## 3. 调试前安全要求

1. 确认调试窗口已获得站控、PCS和BMS负责人的许可。
2. 调试电脑只接入指定控制网络，不开启互联网共享或无线热点。
3. 首轮调试只读，EMS配置中的 `control.enabled` 必须保持为 `false`。
4. Modbus Poll、QModMaster或其他第三方工具只使用FC03，不使用FC05、FC06、FC15、FC16。
5. 先调一套、再调五套、最后扩展到二十套。不要在地址和比例系数未确认时一次性全开。
6. 记录修改前配置、现场设备状态、操作人、时间和回退方法。

## 4. 软件与文件检查

在Windows PowerShell中执行：

```powershell
Set-Location 'D:\work\研发\ems'
node --version
npm.cmd --version
npm.cmd test
```

正常结果：Node.js和npm能显示版本，自动化测试全部通过。

关键文件：

| 文件 | 作用 |
|---|---|
| `vrfb_modbus_config.json` | 真实设备IP、端口、超时、Unit ID、轮询周期 |
| `config/vrfb-point-model.json` | 由V1.2点表生成的运行时寄存器模型 |
| `EMS通信点表V1.2.xlsx` | 单套PCS+BMS点表模板 |
| `modbus-bridge.js` | 真实Modbus TCP采集及WebSocket发布 |
| `tools/check-modbus-tcp.js` | 只读FC03连通性诊断 |
| `mock-bridge.js` | 离线模拟数据，不连接现场设备 |
| `vrb_scada_premium.html` | EMS高级监控界面 |

## 5. 配置现场网络

### 5.1 获取参数

向现场网络负责人取得：

- Modbus服务器IP和子网掩码；
- 网关（同网段直连时通常可留空）；
- TCP端口，通常为502；
- 每套设备的Unit ID；
- 是否限制客户端IP；
- 同时连接数上限和允许的轮询周期。

### 5.2 设置调试电脑网卡

示例：若设备为`192.168.1.100/24`，可临时把调试网卡设为`192.168.1.10`，掩码`255.255.255.0`。不要使用已经被占用的地址。

检查网卡：

```powershell
ipconfig /all
Get-NetIPConfiguration
```

### 5.3 分层检查链路

```powershell
ping 192.168.1.100
Test-NetConnection 192.168.1.100 -Port 502
arp -a
```

判断方法：

- `ping`成功：只能说明IP层基本可达，不代表Modbus服务正常。
- `TcpTestSucceeded : True`：502端口能够建立TCP连接。
- `ping`失败但502成功：设备可能禁用了ICMP，Modbus仍可调试。
- 两者都失败：先检查网线、VLAN、网卡IP、防火墙和设备服务，不要修改点表。

## 6. 配置EMS真实桥接

1. 双击`EMS一键启动.exe`，打开EMS界面。
2. 点击左侧“参数设置”。页面会从`vrfb_modbus_config.json`读取当前配置。
3. 单机初调时只启用待测设备，其余行取消“启用”；也可点击“+ PCS”或“+ BMS”新增独立设备。
4. 设置设备ID、名称、类型、IP/主机名、TCP端口、Unit ID、响应超时、轮询周期、重连间隔和FC03最大长度。
5. 设备通过TCP转RTU网关接入时，勾选“记录串口参数”并填写波特率、数据位、校验位、停止位。注意这些串口值仍须在网关管理页面中设置，EMS通过TCP访问网关时不会直接改变网关串口。
6. 点击该设备的“FC03只读测试”。PASS应显示端点、Unit ID、响应时间和地址1原始值；测试不包含FC06/FC16。
7. 点击“保存参数”。程序原子写入JSON，并把修改前文件备份为`vrfb_modbus_config.json.bak`。
8. 关闭并重新启动真实Modbus通讯，使采集器加载新参数。模拟演示不读取现场设备。

设置页还支持配置JSON导入和导出。导入后必须核对并点击保存；不要把未经确认的设备直接勾选启用。

## 7. 点表与地址核对

### 7.1 重新生成运行时点表

只有在`EMS通信点表V1.2.xlsx`内容变更后才执行：

```powershell
npm.cmd run points:generate
npm.cmd test
```

不要直接手工编辑`config/vrfb-point-model.json`后又忘记同步Excel；Excel是点表源文件。

### 7.2 地址换算示例

若V1.2中某变量地址为233：

- 文档地址：233；
- Modbus PDU起始地址：232；
- 常见调试软件若启用“PLC地址/40001显示”需按软件规则再次确认；
- 本工程运行时模型已处理1基到0基换算，不应再手动减一次。

### 7.3 值的解释

逐项核对：

- 数据类型：无符号16位、有符号16位、32位整数或浮点；
- 字节序和字序：ABCD、BADC、CDAB或DCBA；
- 比例系数：原始值×0.1、×0.01等；
- 单位：V、A、kW、kVar、℃、%、压力和流量单位；
- 状态字：每一位的含义和有效电平；
- 无效值：如`0xFFFF`、`0x7FFF`或厂家自定义哨兵值。

流量与液位目前在V1.2备用变量中，界面仍标记为`raw`；现场提供单位、量程和比例系数后才能取消该标记。

## 8. 第一阶段：单套只读诊断

在设置页只启用一台已确认设备，先点击该行“FC03只读测试”。也可以保存后执行批量命令：

```powershell
Set-Location 'D:\work\研发\ems'
npm.cmd run diagnostics:modbus
```

诊断程序执行：

- 连接配置的IP和端口；
- 设置Unit ID；
- 用FC03读取表内地址1（协议偏移0）的1个寄存器；
- 输出延迟、原始值和PASS/FAIL；
- 不包含任何写寄存器调用。

成功示例的关键字段：

```json
{
  "protocol": "Modbus TCP",
  "operation": "FC03 read-only diagnostic",
  "devices": [{ "deviceId": "BMS001", "target": "192.168.1.100:502", "unitId": 1, "status": "PASS" }],
  "passed": 1,
  "failed": 0
}
```

若地址1在设备中未定义，可能返回异常码02。此时TCP链路本身可能正常，应与厂家确认一个安全且长期有效的FC03测试地址，再调整诊断地址。

## 9. 第二阶段：20套逐台诊断

单套通过后，在设置页分批启用更多设备、保存并执行：

```powershell
npm.cmd run diagnostics:modbus
```

检查输出：

1. `devices`中应包含全部已启用设备。
2. 每项应显示正确的设备ID、IP、端口和Unit ID；不同IP下的Unit ID允许相同。
3. `passed`应为20，`failed`应为0。
4. 对失败的Unit ID单独测试，不要通过增加轮询频率掩盖问题。
5. 若不同Unit ID返回完全相同且不变化的数据，检查网关是否忽略Unit ID或现场地址配置重复。

推荐分批方式：

1. 只启用1台；
2. 启用5台；
3. 启用10台；
4. 最后启用本阶段全部设备。

## 10. 启动真实数据服务

诊断通过后，在PowerShell中运行：

```powershell
Set-Location 'D:\work\研发\ems'
npm.cmd start
```

保持窗口打开。正常时真实桥接逐行连接所有已启用设备，并在`ws://127.0.0.1:8082`发布数据。每行使用自己的IP、端口、Unit ID、超时和轮询周期。

注意：双击`EMS一键启动.exe`启动的是只读模拟数据，适用于界面演示，不是现场真实Modbus采集。真实调试必须启动`npm.cmd start`，并确保界面顶部显示“实时数据 · MODBUS TCP”，而不是“模拟数据 · READ ONLY”。

若8082被占用：

```powershell
Get-NetTCPConnection -LocalPort 8082 -ErrorAction SilentlyContinue
Get-Process -Id (Get-NetTCPConnection -LocalPort 8082).OwningProcess
```

先确认占用进程是否为旧的EMS桥接。不要直接结束未知生产进程。

## 11. 打开界面并切换设备

### 11.1 模拟数据检查

双击项目根目录的`EMS一键启动.exe`。浏览器打开后，顶部应显示模拟数据标识。模拟模式应出现20套PCS和20套BMS，适合检查页面布局和选择器。

### 11.2 真实数据检查

真实桥接运行后，打开已部署的`vrb_scada_premium.html`。若使用当前本机预览地址，可在网址中指定WebSocket：

```text
http://127.0.0.1:8090/vrb_scada_premium.html?ws=ws://127.0.0.1:8082
```

界面操作：

1. 点击左侧“PCS 变流器”。
2. 顶部设备条选择PCS01～PCS20任意一台。
3. 核对标题中的设备名和“Modbus从站”编号。
4. 检查网侧电压、逆变侧电压、直流电压/电流、NPC上下电容和开关反馈。
5. 点击左侧“BMS 电池系统”。
6. 顶部设备条选择BMS01～BMS20任意一套。
7. 核对SOC、电堆电压/电流、A/B分区电压、压力、温度、流量和液位。
8. PCS和BMS选择彼此独立；例如可同时保持PCS03和BMS17，便于交叉排查。
9. 切到“通信与点表”，核对数据质量、点表版本和只读状态。
10. 切到“参数设置”，可重新读取、编辑、FC03测试、导入导出和保存设备参数；保存后重启真实通讯。
11. 在“BMS 电池系统”页面右上角选择数据通道：
    - “真实Modbus通道”连接`ws://127.0.0.1:8082`，显示现场采集数据；
    - “模拟数据通道”连接`ws://127.0.0.1:8081`，只用于界面演示；
    - 顶部必须显示“实时数据 · MODBUS TCP”才能把BMS数值作为现场数据使用。

通道切换只改变当前页面的数据来源，不会启动或停止采集进程。切换到真实通道前，应先启动真实Modbus通讯并确认8082正在监听。

## 12. 使用第三方Modbus主站交叉验证

可使用Modbus Poll、QModMaster或现场认可的同类工具。配置原则：

- Connection：Modbus TCP/IP；
- IP：现场服务端地址；
- Port：502；
- Unit/Slave ID：先1，再逐一到20；
- Function：03 Read Holding Registers；
- Address：按软件是否0基显示选择；
- Quantity：先1～10，确认后再扩大；
- Scan Rate：建议1000 ms或更慢。

同一测点至少完成“三方比对”：设备本地HMI值、第三方工具原始值、EMS界面工程值。三者不一致时先比较原始寄存器，再查比例系数、符号和字节序。

## 13. Wireshark抓包方法

建议在允许抓包的调试电脑上使用显示过滤器：

```text
tcp.port == 502
```

重点检查：

- TCP三次握手是否成功；
- 请求和响应的Transaction ID是否对应；
- Protocol ID是否为0；
- Unit Identifier是否为当前选择的1～20；
- Function Code是否为03；
- 起始地址和寄存器数量是否正确；
- 异常响应的功能码是否为`0x83`；
- 是否发生大量TCP重传、零窗口或RST。

Modbus TCP没有串口CRC字段，不要把RTU的CRC问题套用到TCP报文。

## 14. 常见故障排查

| 现象 | 常见原因 | 处理顺序 |
|---|---|---|
| `ECONNREFUSED` | 目标端口未监听或被防火墙拒绝 | 查设备Modbus服务、端口、防火墙 |
| `ETIMEDOUT` | 路由/VLAN错误、设备无响应、超时太短 | 查网线/IP/VLAN，再适当增大timeout |
| Unit 1成功，其余失败 | 网关只配置了一个从站或Unit ID不符 | 核对网关映射和设备清单 |
| 异常码01 | 设备不支持该功能码 | 确认使用FC03及厂家协议 |
| 异常码02 | 起始地址或范围非法 | 核对1基/0基和最大地址 |
| 异常码03 | 数量或数据值非法 | 缩小单次读取数量 |
| 异常码04 | 从站内部故障 | 查设备告警和厂家日志 |
| 值放大10/100倍 | 比例系数错误 | 对照点表scale |
| 功率/电流正负相反 | 有符号解释或充放电方向定义不同 | 核对int16及符号约定 |
| 32位数剧烈异常 | 字序/字节序错误 | 用两个原始寄存器验证ABCD/CDAB等 |
| 多台显示同一值 | Unit ID重复或网关忽略Unit ID | 逐台断开/改变状态核对 |
| 页面显示`--` | 测点缺失、质量BAD_COMM或映射名称不匹配 | 查桥接日志、快照质量、点表模型 |
| 页面显示模拟数据 | 启动了mock桥而非真实桥 | 停止mock，运行`npm.cmd start` |
| WebSocket断开 | 8082端口冲突、桥接退出或URL错误 | 查进程、终端日志和页面ws参数 |

## 15. 性能和轮询验收

20个Unit ID共用一个TCP连接并顺序轮询。V1.2点表会按连续地址块拆分读取，单次不超过125个寄存器。因此配置中的`polling.intervalMs: 1000`是“一轮结束后的等待”，实际全站刷新周期还包括20套设备的全部请求响应时间。

现场应记录：

- 单个FC03平均/最大响应时间；
- 一轮20套采集总耗时；
- 10分钟内超时次数；
- TCP重连次数；
- 每套设备数据时间戳差异；
- 页面从现场状态变化到显示变化的端到端延迟。

若一轮时间过长，先减少非必要读取块或与厂家协商并发/多连接能力，不要盲目把轮询间隔降到很小。

## 16. 建议验收记录表

| 时间 | 设备 | IP:端口 | Unit ID | FC03 | 原始地址/值 | EMS工程值 | HMI值 | 延迟ms | 结果 | 备注 |
|---|---|---|---:|---|---|---|---|---:|---|---|
| | PCS01/BMS01 | | 1 | | | | | | | |
| | PCS02/BMS02 | | 2 | | | | | | | |
| … | … | … | … | … | … | … | … | … | … | … |
| | PCS20/BMS20 | | 20 | | | | | | | |

每套至少核对：一个模拟量、一个有符号量、一个状态字、SOC、一个分区电压、一个压力、温度、流量和液位。

## 17. 调试完成判据

同时满足以下条件才算本阶段通讯调试通过：

1. 20个Unit ID的FC03只读诊断全部PASS。
2. 页面显示20套PCS和20套BMS。
3. PCS01、PCS20、BMS01、BMS20均可独立切换。
4. 每套设备名称、Unit ID和现场柜号一一对应。
5. 关键点原始值与第三方工具一致，工程值与HMI在允许误差内一致。
6. 流量和液位的单位、比例系数已由BMS厂家书面确认；未确认前保持raw标记。
7. 连续运行至少2小时无持续超时、异常重连或数据串台。
8. 抓包中只有批准的FC03读取，没有FC06/FC16写请求。
9. 配置文件、点表、测试记录和抓包文件完成版本归档。

## 18. 扩展到100套BMS和50套PCS前需要补充的资料

本阶段只实现用户指定的20套PCS和20套BMS。扩展到全站前，请提供：

1. 50套PCS的设备编号、IP、端口、Unit ID和额定功率。
2. 100套BMS的设备编号、IP、端口、Unit ID，以及与PCS的配对/汇聚关系。
3. 是否存在一台PCS对应两套BMS；若存在，需要明确功率、SOC和告警的汇总规则。
4. 网关数量、并发连接上限、轮询频率限制和网络分区方案。
5. 流量、液位、压力的单位、量程、比例系数及无效值定义。
6. 现场允许的总刷新周期和关键点优先级。

资料齐全后，应把当前“同IP、同Unit配对”的简化模型升级为显式设备清单，而不是把Unit ID简单扩展到100。
